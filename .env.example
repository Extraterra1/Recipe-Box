# Required for cloud sync and recipe URL import. URL import also requires an
# online, authenticated user and the import-recipe Edge Function.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key
